apiVersion: v1
kind: Service
metadata:
  name: finedge-api-service
  namespace: default
spec:
  selector:
    app: finedge-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
